apiVersion: v1
kind: Service
metadata:
  name: tienda-backend
  namespace: tienda
spec:
  type: LoadBalancer
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
  selector:
    app: tienda-backend
